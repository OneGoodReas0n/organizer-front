/*
 * Support Messages
 *
 * This contains all the text for the Support container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Support';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Support container!',
  },
});
